{
  "name": "null-horizon-server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "PORT=3001 bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "PORT=3001 bun run src/index.ts",
    "lint": "oxlint"
  },
  "dependencies": {
    "hono": "^4.9.1"
  },
  "devDependencies": {
    "@repo/typescript-config": "*",
    "bun-types": "^1.2.20",
    "typescript": "5.5.4"
  }
}
