{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "oxlint --type-aware",
    "lint:fix": "oxlint --type-aware --fix",
    "lint:basic": "oxlint",
    "lint:type-check": "turbo run type-check",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "oxlint": "^1.12.0",
    "oxlint-tsgolint": "^0.0.4",
    "prettier": "^3.2.5",
    "turbo": "^2.5.6"
  },
  "name": "with-vite",
  "packageManager": "bun@1.2.20",
  "workspaces": [
    "apps/**/*",
    "packages/*"
  ]
}
